package com.securetrack.backend.models;

/**
 * Role enumeration for Staff members.
 * Represents the different roles that staff members can have within the system.
 */
public enum StaffRole {
    ADMIN,
    CUSTOM_OFFICER,
    INSPECTOR
}
